<section class="hero">
    <div class="hero-body">
        <div class="container">
            <h1 class="title">
                <i class="fas fa-at"></i><%= user.username %>
            </h1>
            <h2 class="subtitle">
                <%= user.name %> | <%= user.public_repos %> repository
            </h2>

            <a class="button is-dark is-outlined" href="/">
                <span class="icon">
                    <i class="fas fa-arrow-left"></i>
                </span>
                <span>Back</span>
            </a>
        </div>
    </div>
</section>